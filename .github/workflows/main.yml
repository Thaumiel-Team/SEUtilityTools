name: SEUtilityTools Auto Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name != github.event.pull_request.base.repo.full_name

    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4.3.1
        with:
          dotnet-version: '10.0.x'

      - name: Publish — Windows
        run: dotnet publish SEUtilityTools.csproj --configuration WINDOWS --output publish/Windows

      - name: Publish — Windows (Self-Contained)
        run: dotnet publish SEUtilityTools.csproj --configuration WINDOWS-SELF --output publish/Windows-SC

      - name: Publish — Linux
        run: dotnet publish SEUtilityTools.csproj --configuration LINUX --output publish/Linux

      - name: Publish — Linux (Self-Contained)
        run: dotnet publish SEUtilityTools.csproj --configuration LINUX-SELF --output publish/Linux-SC

      - name: Upload Windows
        uses: actions/upload-artifact@v4
        with:
          name: SEUtilityTools-Windows
          path: publish/Windows/

      - name: Upload Windows (Self-Contained)
        uses: actions/upload-artifact@v4
        with:
          name: SEUtilityTools-Windows-SC
          path: publish/Windows-SC/

      - name: Upload Linux
        uses: actions/upload-artifact@v4
        with:
          name: SEUtilityTools-Linux
          path: publish/Linux/

      - name: Upload Linux (Self-Contained)
        uses: actions/upload-artifact@v4
        with:
          name: SEUtilityTools-Linux-SC
          path: publish/Linux-SC/

      - name: Create draft release
        if: github.event_name == 'push'
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          tag_name: build-${{ github.run_number }}
          name: "Build #${{ github.run_number }} — ${{ github.sha }}"
          body: |
            Automated draft release from commit ${{ github.sha }}.

            | File | Description |
            |---|---|
            | `SEUtilityTools-Windows.exe` | Windows — requires .NET 10 runtime |
            | `SEUtilityTools-Windows-SC.exe` | Windows — self-contained (no runtime needed) |
            | `SEUtilityTools-Linux` | Linux — requires .NET 10 runtime |
            | `SEUtilityTools-Linux-SC` | Linux — self-contained (no runtime needed) |

            **To publish:** edit this release, set a proper version tag, update the notes, and hit Publish.
          files: |
            publish/Windows/SEUtilityTools-Windows.exe
            publish/Windows-SC/SEUtilityTools-Windows-SC.exe
            publish/Linux/SEUtilityTools-Linux
            publish/Linux-SC/SEUtilityTools-Linux-SC
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN  }}
